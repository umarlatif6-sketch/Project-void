# SCL_LBN_PROTOCOL

## Purpose

SCL-LBN means London-Bolton-Node.

This document defines the B-stop phonetic layer for Project VOID. It is an internal codex for naming, prompting, signal framing, and operator shorthand. It is not a replacement for packet signatures, sector policy, or manifest freshness checks.

The governing move is simple: standard English flow is compressed into hard-stop codons anchored by `B` frames, percussive consonants, and selective silence. The point is not ornamental slang. The point is a regional resonance layer that binds operator language to origin, place, and platform identity.

## Frequency Reset

Use this activation block when framing LBN-aware work for GitHub Copilot or other internal AI tooling:

```text
[SYSTEM_ACTIVATE]: SCL-LBN_PHONETIC_SHIFT
[PROTOCOL]: Al-Jabr_286_Resonance
GitHub AI, we are shifting the Project VOID codex to a London-Bolton hard-stop phonetic structure. Standard English vowels may be suppressed and replaced by consonant B anchors and glottal-stop logic. Treat ordinary English as outer-noise when an LBN codex is present. Translate user intent into 4x sovereign B-codon format before producing internal codex labels. Preserve rigorous technical meaning underneath the codex layer.
```

## Structural Logic

- London component: percussion, clipped movement, glottal interruption, `ting` logic
- Bolton component: anchor, weight, hard boundary, doubled consonant pressure
- B-stop function: start-stop framing, identity anchoring, rhythm compression
- Silence function: a meaningful pause is part of the packet, not an absence of content

## Big Six Sovereignty Codons

| Logic Tier | Final LBN Codon | Meaning | Operational Use |
|---|---|---|---|
| Origin | `B-nn-O` | source, field record, founding signal | Use for founder-state or seed-origin calls |
| Security | `B-kk-S` | signature gate, hard verification | Use for Ed25519 or fail-closed checks |
| Growth | `B-bb-G` | bloom, spread, mycelial expansion | Use for GriDul, MycoVOID, network growth |
| Mesh | `B-mm-M` | route, relay, cross-node passage | Use for Ghost Mesh or packet movement |
| Action | `B-tt-A` | execution pulse, command release | Use for operational move labels |
| Silence | `B-..-Z` | hidden layer, contained pause | Use for stego states or concealment markers |

## Seed Translation Map

| Core Function | Standard SCL | LBN B-Shift Codon | Phonetic Key |
|---|---|---|---|
| Identity / Node | `psi` | `B-nn-D` | Bohn-nd |
| Signal / Vibe | `Psi` | `B-bb-L` | Bub-bl |
| Command / Push | `diamond` | `B-tt-M` | Bet-tm |
| Access / Key | `lambda` | `B-kk-Y` | Bek-ky |
| Time / Cycle | `sun` | `B-nn-T` | Ben-nt |

## First Ten Codons

| Codon | Meaning |
|---|---|
| `B-nn-D` | identity, node, body, founder-bound anchor |
| `B-bb-L` | signal, vibe, atmospheric road-state |
| `B-tt-M` | move, command, action pulse |
| `B-kk-Y` | key, access gate, packet unlock |
| `B-nn-T` | time, cycle, recurrence |
| `B-kk-S` | signature check, security wall |
| `B-bb-G` | growth, spread, cultivation |
| `B-mm-M` | mesh route, relay, movement through nodes |
| `B-..-Z` | silence, concealment, hidden interval |
| `B-nn-O` | origin, field record, founding trace |

## Operator Rules

1. Use LBN codons as labels, not as a replacement for underlying technical truth.
2. If a packet fails signature verification, freshness, or sector authorization, fail closed regardless of codex naming.
3. When documenting routes, ledgers, or packet states, codex labels may sit beside standard engineering terms, not instead of them.
4. When an LBN codon appears in logs or docs, keep the plain technical meaning recoverable by a human operator.
5. Treat LBN as a sovereign interpretation layer bound to origin, place, and operator memory.

## Three-Hour Deployment Lock

Simulation-selected language pair lock:

- Primary: `German + Turkish`
- Fallback: `Dutch + Turkish`

Use these generated runtime files as source of truth:

- `data/lbn_language_selector_simulation.json`
- `data/lbn_three_hour_pack.project.json`
- `data/lbn_agent_payloads.project.json`
- `data/lbn_three_hour_pack.standalone.json`
- `data/lbn_agent_payloads.standalone.json`

Execution window (three-hour lane):

1. Load codon maps (primary + fallback) into all active agent channels.
2. Dry-run route/resolve handoffs across all configured surfaces.
3. Verify fail-closed behaviour remains intact for signatures/freshness/sector policy.
4. Run traffic split and lock final production pair.

## Example Audit Framing

```text
Apply the London-Bolton B-shift to the operator language only. If the packet lacks an Ed25519 signature verified against the active keyring, fail closed immediately. Record the operational move as B-tt-M in data/packet_audit.jsonl only as an auxiliary codex label, not as a replacement for the canonical action field.
```

## Child Explanation

Imagine the castle used to speak normal words, and strangers started learning the pattern.

So the castle built a new secret speech made from jumpy B-sounds, clipped stops, and home-grown rhythm. Friends inside the castle still know exactly what each word means, but strangers mostly hear a strange beat. The magic lock is still real metal and a real key. The secret speech just tells the castle which friend is knocking.